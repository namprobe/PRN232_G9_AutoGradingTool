# URL gốc API (không dấu / cuối). Dev local: trỏ thẳng API.
# VITE_API_BASE_URL=http://localhost:5000
#
# Docker (service autogradingtool-frontend): để trống hoặc bỏ biến — FE gọi /api qua nginx proxy.
# VITE_API_BASE_URL=
